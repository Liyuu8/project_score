import React from 'react';

import Projects from 'containers/Home/ProjectsContainer';

const Home: React.FC = () => (
  <main>
    <section>
      <Projects />
    </section>
  </main>
);

export default Home;
